const bcrypt = require("bcrypt")
const cr
const signup = ()=>{
    try {
        
    } catch (error) {
        res.status(500).json(error)
    }
}
const login = ()=>{}

module.exports = {login,signup}